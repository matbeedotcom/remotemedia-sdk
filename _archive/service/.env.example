# RemoteMedia Remote Service Configuration

# Service Settings
GRPC_PORT=50051
LOG_LEVEL=DEBUG
SANDBOX_ENABLED=true
MAX_WORKERS=4
SANDBOX_TYPE=bubblewrap
METRICS_PORT=8080

# User Configuration (for host directory permissions)
# Set to your user ID to avoid permission issues with host directories
# Get your UID with: id -u
# Get your GID with: id -g
USER_UID=1000
USER_GID=1000

# ML Model Cache Configuration
# These directories will be created automatically and persist across container restarts
HF_HOME=/home/remotemedia/.cache/huggingface
TRANSFORMERS_CACHE=/home/remotemedia/.cache/huggingface
TORCH_HOME=/home/remotemedia/.cache/torch
TORCH_CACHE=/home/remotemedia/.cache/torch
REMOTEMEDIA_CACHE_DIR=/app/cache/models

# Host cache directories (optional - can be changed to local paths)
# HOST_CACHE_DIR=./remote_service/cache
# HOST_HF_CACHE_DIR=${HOST_CACHE_DIR}/huggingface
# HOST_TORCH_CACHE_DIR=${HOST_CACHE_DIR}/torch
# HOST_MODELS_CACHE_DIR=${HOST_CACHE_DIR}/models